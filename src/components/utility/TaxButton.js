import React from 'react';

const TaxButton = ({ }) => {
  return (
    <div>
      <button onClick={history.goBack} className="main-button">
        
      </button>
    </div>
  );
};

export default TaxButton;
